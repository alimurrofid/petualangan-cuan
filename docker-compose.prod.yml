services:
  frontend:
    image: ghcr.io/alimurrofid/petualangan-cuan/frontend:latest
    container_name: prod_frontend
    environment:
      - VITE_API_URL=${FRONTEND_URL}/api
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    networks:
      - app-network
    restart: always

  backend:
    image: ghcr.io/alimurrofid/petualangan-cuan/backend:latest
    container_name: prod_backend
    environment:
      - PORT=${PORT}
      - DB_HOST=${DB_HOST:-postgres}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DB_PORT=5432
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ACCESS_EXPIRY=${JWT_ACCESS_EXPIRY}
      - JWT_REFRESH_EXPIRY=${JWT_REFRESH_EXPIRY}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REDIRECT_URL=${GOOGLE_REDIRECT_URL}
      - FRONTEND_URL=${FRONTEND_URL}
      - LOCAL_LLM_URL=${LOCAL_LLM_URL}
      - LOCAL_WHISPER_URL=${LOCAL_WHISPER_URL}
      - AI_PROVIDER=${AI_PROVIDER}
      - EXTERNAL_AI_URL=${EXTERNAL_AI_URL}
      - EXTERNAL_AI_API_KEY=${EXTERNAL_AI_API_KEY}
      - EXTERNAL_AI_MODEL=${EXTERNAL_AI_MODEL}
      - WA_GATEWAY_URL=${WA_GATEWAY_URL}
      - WA_GATEWAY_USERNAME=${WA_GATEWAY_USERNAME}
      - WA_GATEWAY_PASSWORD=${WA_GATEWAY_PASSWORD}
      - WHATSAPP_WEBHOOK_SECRET=${WHATSAPP_WEBHOOK_SECRET}
      - TZ=Asia/Jakarta
    ports:
      - "${BACKEND_PORT:-8080}:${PORT}"
    volumes:
      - ./uploads_prod:/app/uploads
    depends_on:
      postgres:
        condition: service_started
      llm-server:
        condition: service_started
      whisper-server:
        condition: service_started
    networks:
      - app-network
    restart: always
    user: appuser
  
  postgres:
    image: postgres:15-alpine
    container_name: prod_db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    networks:
      - app-network
    restart: always

  llm-server:
    image: ghcr.io/ggml-org/llama.cpp:server
    container_name: prod_llm
    volumes:
      - ../shared/ai-models:/models
    command: >
      -m /models/google_gemma-3-4b-it-Q4_K_M.gguf
      --mmproj /models/mmproj-google_gemma-3-4b-it-f16.gguf
      --host 0.0.0.0
      --port 8080
      -c 4096
      -t 2
    deploy:
      resources:
        limits:
          memory: 4G
    ports:
      - "${LLM_PORT:-8081}:8080"
    networks:
      - app-network
    restart: always

  whisper-server:
    image: fedirz/faster-whisper-server:latest-cpu
    container_name: prod_whisper
    ports:
      - "${WHISPER_PORT:-8000}:8000"
    volumes:
      - ../shared/ai-models/whisper:/root/.cache/huggingface
    environment:
      - WHISPER_MODEL=small
    networks:
      - app-network
    restart: always

  wa-gateway:
    image: ghcr.io/alimurrofid/petualangan-cuan/wa-gateway:latest
    container_name: prod_wa_gateway
    ports:
      - "${WA_GATEWAY_PORT:-3003}:3000"
    environment:
      - APP_BASIC_AUTH=${WA_GATEWAY_USERNAME:-admin}:${WA_GATEWAY_PASSWORD:-admin}
      - WHATSAPP_WEBHOOK=${WA_WEBHOOK_URL}
      - WHATSAPP_WEBHOOK_SECRET=${WHATSAPP_WEBHOOK_SECRET}
      - WHATSAPP_WEBHOOK_EVENTS=message
      - WHATSAPP_AUTO_MARK_READ=true
    volumes:
      - ./wa-gateway-data_prod:/app/data
    networks:
      - app-network
    restart: always

  loki:
    image: grafana/loki:latest
    container_name: prod_loki
    ports:
      - "${LOKI_PORT:-3100}:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - loki_data_prod:/loki
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

  promtail:
    image: grafana/promtail:latest
    container_name: prod_promtail
    user: root
    privileged: true
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./monitoring/promtail-config.yml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M

  grafana:
    image: grafana/grafana-oss:10.4.1
    container_name: prod_grafana
    ports:
      - "${GRAFANA_PORT:-3002}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_ANALYTICS_REPORTING_ENABLED=false
      - GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/etc/grafana/provisioning/dashboards/petualangan-cuan.json
    volumes:
      - grafana_data_prod:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - loki
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: prod_uptime
    ports:
      - "${UPTIME_KUMA_PORT:-3001}:3001"
    volumes:
      - uptime_kuma_data_prod:/app/data
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data_prod:
  loki_data_prod:
  grafana_data_prod:
  uptime_kuma_data_prod:
