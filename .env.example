# Server Configuration
PORT=8080
TZ=Asia/Jakarta

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=petualangan_cuan
DB_PORT=5432

# Security & Authentication (JWT)
# Generate a secure key using: openssl rand -base64 32
JWT_SECRET=your_generated_jwt_secret_here
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=72h

# Google OAuth 2.0 Configuration
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret
GOOGLE_REDIRECT_URL=http://localhost:8080/api/auth/google/callback

# Frontend Integration (CORS)
FRONTEND_URL=http://localhost:5173

# AI Provider Configuration
# Pilih provider: "local" (default) atau "external"
AI_PROVIDER=local

# Local LLM (digunakan saat AI_PROVIDER=local)
LOCAL_LLM_URL=http://llm-server:8080

# Whisper Services (Local Transcription)
LOCAL_WHISPER_URL=http://whisper-server:8000

# External AI API (digunakan saat AI_PROVIDER=external)
# Contoh: OpenRouter, OpenAI, Groq, dsb.
EXTERNAL_AI_URL=https://openrouter.ai/api/v1/chat/completions
EXTERNAL_AI_API_KEY=your_external_api_key_here
EXTERNAL_AI_MODEL=google/gemini-2.0-flash-001

# WA Gateway
WA_GATEWAY_USERNAME=yourusername
WA_GATEWAY_PASSWORD=yourpassword
WA_DB_DRIVER=sqlite
WA_WEBHOOK_URL=http://petualangan_cuan_backend:8080/api/webhook/whatsapp
# Generate a secure hook secret using: openssl rand -base64 32
WHATSAPP_WEBHOOK_SECRET=your_webhook_secret
WA_GATEWAY_URL=http://petualangan_cuan_wa_gateway:3000

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=admincuan

# Docker Host Port
FRONTEND_PORT=3000
BACKEND_PORT=8080
LLM_PORT=8081
WHISPER_PORT=8000
WA_GATEWAY_PORT=3003
LOKI_PORT=3100
GRAFANA_PORT=3002
UPTIME_KUMA_PORT=3001
